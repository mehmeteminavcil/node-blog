<!DOCTYPE html>
<html lang="en">
  <head>
    <%-include('./inc/head') %>
  </head>
  <body>
    <%-include('./inc/nav') %>

    <section>
      <h1>Yazilar</h1>
      <ul>
        <% blogs.forEach(blog => { %>

        <li>
          <a href="/blog/<%=blog._id %>"><%=blog.title %> </a>
          <p><%=blog.short %></p>
          <p><%=blog.long %></p>
        </li>
        <% })%>
      </ul>
    </section>

    <footer><%-include('./inc/footer') %></footer>
  </body>
</html>
